### This file was autogenerated by GraphQL Nexus
### Do not make changes to this file directly


type Address {
  city: String!
  country: String!
  formattedAddress: String!
  state: String!
  street: String!
  streetNumber: String!
  timeZone: String!
  zipCode: String!
}

type AuthPayload {
  token: String!
  user: User!
}

type Coords {
  latitude: Float!
  longitude: Float!
}

"""Input type for logging in a user"""
input CoordsFilterInput {
  gte: Float!
  lte: Float!
}

type Court {
  address: Address!
  coords: Coords!
  id: ID!
  name: String!
  numberOfCourts: String
  phone: String
  type: [CourtTypeEnum!]!
}

"""Input type for logging in a user"""
input CourtsFilterInput {
  latitude: CoordsFilterInput!
  longitude: CoordsFilterInput!
}

"""Different types of available courts"""
enum CourtTypeEnum {
  Indoor
  Outdoor
}

"""Input type for creating a court"""
input CreateCourtInput {
  address: String!
  courtType: [CourtTypeEnum!]!
  name: String!
  numberOfCourts: Int
  phone: String
  suggestedCourtId: ID!
  timeZone: String!
}

"""Required input for creating a session"""
input CreateSessionInput {
  courtId: ID!
  end: DateTime!
  start: DateTime!
}

"""
A date string, such as 2007-12-03, compliant with the `full-date` format
outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for
representation of dates and times using the Gregorian calendar.
"""
scalar Date

"""
A date-time string at UTC, such as 2007-12-03T10:15:30Z, compliant with the
`date-time` format outlined in section 5.6 of the RFC 3339 profile of the ISO
8601 standard for representation of dates and times using the Gregorian calendar.
"""
scalar DateTime

"""Input type for deleting a suggested court"""
input DeleteSuggestedCourtInput {
  suggestedCourtId: ID!
}

"""Required input for to join/unjoin a session"""
input JoinUnjoinSessionInput {
  sessionId: ID!
}

"""Input for logging in a user"""
input LoginInput {
  password: String!
  username: String!
}

type Mutation {
  """Login for an admin account"""
  adminLogin(input: LoginInput!): AuthPayload!
  createCourt(input: CreateCourtInput!): Court!
  createSession(input: CreateSessionInput!): Session!
  deleteSuggestedCourt(input: DeleteSuggestedCourtInput!): Boolean!
  joinSession(input: JoinUnjoinSessionInput!): Session!

  """Login a user"""
  login(input: LoginInput!): AuthPayload!
  logout: Boolean!

  """Sign up new user"""
  signup(input: SignUpInput!): AuthPayload!
  suggestCourt(input: SuggestCourtInput!): SuggestedCourt!
  unjoinSession(input: JoinUnjoinSessionInput!): Session!
  updateSession(input: UpdateSessionInput!): Session!
  updateUser(input: UpdateUserInput!): User!
}

scalar Password

type Query {
  courts(where: CourtsFilterInput): [Court!]!
  sessions(where: SessionsFilterInput): [Session!]!
  suggestedCourts: [SuggestedCourt!]!
}

type Session {
  attending: [User!]!
  coords: Coords!
  court: Court!
  createdBy: User!
  date: String!
  end: DateTime!
  id: ID!
  numberAttending: Int!
  start: DateTime!
  times: String!
  timeZone: String!
}

"""Input type for logging in a user"""
input SessionsFilterInput {
  latitude: CoordsFilterInput!
  longitude: CoordsFilterInput!
}

"""Input type for signing up a user"""
input SignUpInput {
  avatar: Upload
  confirmPassword: Password!
  fullName: String!
  password: Password!
  username: String!
}

"""Input type for suggesting a court"""
input SuggestCourtInput {
  city: String!
  name: String!
  state: String!
  street: String!
  timeZone: String!
  zipCode: String!
}

type SuggestedCourt {
  city: String!
  id: ID!
  name: String!
  state: String!
  street: String!
  timeZone: String!
  zipCode: String!
}

"""Required input for updating a session"""
input UpdateSessionInput {
  end: DateTime
  sessionId: ID!
  start: DateTime
}

"""Input for updating a user"""
input UpdateUserInput {
  avatar: Upload
  fullName: String
  userId: ID!
  username: String
}

"""The `Upload` scalar type represents a file upload."""
scalar Upload

type User {
  avatarUrl: String
  fullName: String!
  id: ID!
  username: String!
}
